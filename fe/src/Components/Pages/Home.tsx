import { Button, Card, Heading } from "rebass"

export const Home = () => {
    return <div>
                <Button>Home</Button>
                <Card width={256}>
                    <Heading>Card</Heading>
                </Card>
            </div>
}